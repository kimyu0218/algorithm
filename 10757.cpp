#include <iostream>
#include <string>
#include <stack>

using namespace std;

string compute(const string a, const string b) {
    stack<string> big, small, result;

    return "";
}

int main() {
    string a, b;
    cin >> a >> b;
    cout << compute(a,b);
    return 0;
}